title: Příslušentví
@extends: 
  type: default
  context: blueprints://pages 

form:
  fields:
    tabs:
      type: tabs
      active: 1

      fields:
        content:
          type: tab

          #
          # Add your custom fields here.
          # These fields will be available in the page's Content tab
          # inside the Admin Panel.
          #
          fields:

            #
            # Custom text field
            #
            header.custom.color:
              type: text
              label: Barva

            header.custom.popis:
              type: text
              label: Popis

            header.custom.price:
              type: text
              label: Cena

            header.custom.price_dph:
              type: text
              label: Cena DPH